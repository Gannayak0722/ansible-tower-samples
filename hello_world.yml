---
# This playbook prints a simple debug message
- name: ref-resolver-example
  hosts: localhost

  tasks:
  - name: Print name
    debug:
      msg: "{{ name }}"
  
  - name: Print location
    debug:
      msg: "{{ location }}"
